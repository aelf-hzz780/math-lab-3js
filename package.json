{
  "name": "forma-math-lab",
  "private": true,
  "version": "1.2.1",
  "type": "module",
  "scripts": {
    "start": "node serve.js",
    "test": "node --test tests/*.test.js",
    "verify": "node --test tests/*.test.js",
    "build": "node scripts/build.mjs",
    "check:offline": "node scripts/offline-check.mjs",
    "capture:examples": "node scripts/capture-examples.mjs"
  },
  "devDependencies": {
    "esbuild": "0.25.10"
  },
  "description": "Eighteen interactive Three.js experiments with Fields Medal topics, iridescent procedural terrain, offline rendering and reproducible visual examples.",
  "engines": {
    "node": ">=22"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/aelf-hzz780/math-lab-3js.git"
  }
}
